# DreamPlay Store

Next.js 15 + SQLite store. Concept theme mosaic layout.

## Setup
```bash
pnpm install
pnpm seed      # scan local assets → populate SQLite
pnpm dev       # http://localhost:4000
```
